



spiel(){
foreach(karteaufderhand)
    if(handkarten){}
        legeHandkarte()
        berechneSpiel()
        if(punke>maxpunkte)
            maxpunkte=punkte;        
        spiel(n-1)
    }
    else
        return pun
}






spiel(){
    foreach(Karteinderhand){
    legeHandkarte();
        foreach(spieler)
            allekombinationen()               
        if(teamstich)
            return punkte;
        punkte+=spiel(Handkarten-1)
        
    }
}

