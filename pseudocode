



spiel(){
foreach(karteaufderhand)
    if(handkarten){}
        legeHandkarte()
        berechneSpiel()
        if(punke>maxpunkte)
            maxpunkte=punkte;        
        return spiel(n-1)
    }
    else
        return punkte
}






spiel(){
    foreach(Karteinderhand){
    legeHandkarte();
        foreach(spieler)
            allekombinationen()               
        if(teamstich)
            return punkte;
        punkte+=spiel(Handkarten-1)
        
    }
}

